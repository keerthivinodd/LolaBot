# LolaBot
Basic RAG model to generate information from an uploaded PDF or Text Document and also uses its own thinking ability to answer questions outside of the uploaded document.
# AI Document QA Bot (Powered by Groq & RAG)

An ultra-fast Document Q&A system using **Groq (Llama 3.3)** and **LangChain**. It uses Retrieval-Augmented Generation (RAG) to handle large documents within free-tier API limits.

## 🚀 Setup Instructions

1. **Clone the project** or unzip the folder.
2. **Install Dependencies**:
   ```bash
   pip install streamlit langchain-groq langchain-huggingface langchain-community faiss-cpu sentence-transformers python-dotenv
